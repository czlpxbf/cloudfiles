<Window x:Class="Cloudfiles.App.Views.VersionHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="版本历史"
        Width="550" Height="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1e1e2e">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="版本历史" Foreground="#cdd6f4" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <!-- Version List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Versions}" Background="Transparent" BorderBrush="#45475a" Foreground="#cdd6f4">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="#cdd6f4" />
                    <Setter Property="BorderBrush" Value="#313244" />
                    <Setter Property="Padding" Value="8,6" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="#" Width="40"
                                    DisplayMemberBinding="{Binding Index}" />
                    <GridViewColumn Header="创建时间" Width="180"
                                    DisplayMemberBinding="{Binding CreatedAt}" />
                    <GridViewColumn Header="大小" Width="100"
                                    DisplayMemberBinding="{Binding FormattedSize}" />
                    <GridViewColumn Header="操作" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="下载" FontSize="12" Padding="8,4"
                                        Background="#89b4fa" Foreground="#1e1e2e" BorderThickness="0"
                                        FontWeight="SemiBold" Cursor="Hand"
                                        Click="DownloadVersion_Click" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Close Button -->
        <Button Grid.Row="2" Content="关闭" HorizontalAlignment="Right"
                Click="Close_Click"
                Style="{StaticResource ActionButton}" Margin="0,16,0,0" />
    </Grid>
</Window>
