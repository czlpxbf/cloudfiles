<UserControl x:Class="Cloudfiles.App.Views.FileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e2e">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="文件列表" Foreground="#cdd6f4" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Breadcrumb}" Foreground="#89b4fa" FontSize="14"
                       VerticalAlignment="Center" Margin="16,0,0,0" />
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="下载" Command="{Binding DownloadItemCommand}"
                    Style="{StaticResource ActionButton}" />
            <Button Content="删除" Command="{Binding DeleteItemCommand}"
                    Style="{StaticResource ActionButton}" />
            <Button Content="刷新" Command="{Binding RefreshCommand}"
                    Style="{StaticResource ActionButton}" />
            <Button Content="新建文件夹" Command="{Binding NewFolderCommand}"
                    Style="{StaticResource ActionButton}" />
        </StackPanel>

        <!-- Error Message -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}"
                   Foreground="#f38ba8" FontSize="13" Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

        <!-- File List -->
        <ListView Grid.Row="3"
                  ItemsSource="{Binding Files}"
                  SelectedItem="{Binding SelectedFile}"
                  Background="Transparent" BorderBrush="#45475a"
                  Foreground="#cdd6f4"
                  Margin="0,8,0,0"
                  MouseDoubleClick="FileList_DoubleClick">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="#cdd6f4" />
                    <Setter Property="BorderBrush" Value="#313244" />
                    <Setter Property="Padding" Value="8,6" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <!-- Right-click Context Menu -->
                <ContextMenu x:Key="ItemContextMenu">
                    <MenuItem Header="下载" Command="{Binding DataContext.DownloadItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                             CommandParameter="{Binding}" />
                    <MenuItem Header="删除" Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                             CommandParameter="{Binding}" />
                    <MenuItem Header="重命名" Command="{Binding DataContext.RenameItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                             CommandParameter="{Binding}" />
                    <Separator />
                    <MenuItem Header="版本历史" Command="{Binding DataContext.ShowVersionHistoryCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                             CommandParameter="{Binding}" />
                </ContextMenu>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" Width="300"
                                    DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="大小" Width="120"
                                    DisplayMemberBinding="{Binding FormattedSize}" />
                    <GridViewColumn Header="版本" Width="100"
                                    DisplayMemberBinding="{Binding VersionDisplay}" />
                    <GridViewColumn Header="修改时间" Width="200"
                                    DisplayMemberBinding="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Navigation -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
            <Button Content="上一级"
                    Command="{Binding NavigateUpCommand}"
                    Style="{StaticResource ActionButton}" />
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="5" Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Foreground="#89b4fa" Background="#313244" />
                <TextBlock Text="加载中..." Foreground="#cdd6f4"
                           FontSize="14" Margin="0,8,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
