<UserControl x:Class="Cloudfiles.App.Views.FileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e2e">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="文件列表" Foreground="#cdd6f4" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Breadcrumb}" Foreground="#89b4fa" FontSize="14"
                       VerticalAlignment="Center" Margin="16,0,0,0" />
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="上传"
                    Style="{StaticResource ActionButton}" />
            <Button Grid.Column="1" Content="下载"
                    Style="{StaticResource ActionButton}" />
            <Button Grid.Column="2" Content="刷新"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ActionButton}" />
            <Button Grid.Column="3" Content="新建文件夹"
                    Style="{StaticResource ActionButton}" />
            <TextBlock Grid.Column="5" Text="打开项目主页"
                       Foreground="#89b4fa" FontSize="13"
                       VerticalAlignment="Center" Cursor="Hand"
                       MouseDown="OpenProjectHome_Click" />
        </Grid>

        <!-- Error Message -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}"
                   Foreground="#f38ba8" FontSize="13" Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

        <!-- File List -->
        <ListView Grid.Row="3"
                  ItemsSource="{Binding Files}"
                  SelectedItem="{Binding SelectedFile}"
                  Background="Transparent" BorderBrush="#45475a"
                  Foreground="#cdd6f4"
                  Margin="0,8,0,0"
                  MouseDoubleClick="FileList_DoubleClick">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="#cdd6f4" />
                    <Setter Property="BorderBrush" Value="#313244" />
                    <Setter Property="Padding" Value="8,6" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" Width="300"
                                    DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="大小" Width="120"
                                    DisplayMemberBinding="{Binding Size, Converter={StaticResource FileSizeConverter}}" />
                    <GridViewColumn Header="类型" Width="100"
                                    DisplayMemberBinding="{Binding IsFolder, Converter={StaticResource FileTypeConverter}}" />
                    <GridViewColumn Header="修改时间" Width="200"
                                    DisplayMemberBinding="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Navigation -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
            <Button Content="上一级"
                    Command="{Binding NavigateUpCommand}"
                    Style="{StaticResource ActionButton}" />
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="5" Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Foreground="#89b4fa" Background="#313244" />
                <TextBlock Text="加载中..." Foreground="#cdd6f4"
                           FontSize="14" Margin="0,8,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
