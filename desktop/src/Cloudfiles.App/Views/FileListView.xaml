<UserControl x:Class="Cloudfiles.App.Views.FileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e2e">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="部署列表" Foreground="#cdd6f4" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding ProjectUrl}" Foreground="#89b4fa" FontSize="14"
                       VerticalAlignment="Center" Margin="16,0,0,0"
                       Cursor="Hand" MouseDown="ProjectUrl_Click" />
        </StackPanel>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Background="#313244" Foreground="#cdd6f4" BorderBrush="#45475a"
                     Padding="10,8" FontSize="14"
                     VerticalContentAlignment="Center">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6,0,0,6" />
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <Button Grid.Column="1" Content="搜索"
                    Command="{Binding SearchCommand}"
                    Style="{StaticResource ActionButton}"
                    Margin="0" />

            <Button Grid.Column="2" Content="刷新"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ActionButton}"
                    Margin="8,0,0,0" />
        </Grid>

        <!-- Error Message -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}"
                   Foreground="#f38ba8" FontSize="13" Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

        <!-- Deployment List -->
        <ListView Grid.Row="3"
                  ItemsSource="{Binding Deployments}"
                  SelectedItem="{Binding SelectedDeployment}"
                  Background="Transparent" BorderBrush="#45475a"
                  Foreground="#cdd6f4"
                  Margin="0,8,0,0">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="#cdd6f4" />
                    <Setter Property="BorderBrush" Value="#313244" />
                    <Setter Property="Padding" Value="8,6" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#313244" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="环境" Width="100"
                                    DisplayMemberBinding="{Binding Environment}" />
                    <GridViewColumn Header="状态" Width="100"
                                    DisplayMemberBinding="{Binding Status}" />
                    <GridViewColumn Header="URL" Width="300"
                                    DisplayMemberBinding="{Binding Url}" />
                    <GridViewColumn Header="创建时间" Width="200"
                                    DisplayMemberBinding="{Binding CreatedOn}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Actions -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="打开项目"
                    Command="{Binding OpenProjectUrlCommand}"
                    Style="{StaticResource ActionButton}" />
            <Button Content="打开部署"
                    Command="{Binding OpenUrlCommand}"
                    Style="{StaticResource ActionButton}" Margin="8,0,0,0" />
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="5" Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Foreground="#89b4fa" Background="#313244" />
                <TextBlock Text="加载中..." Foreground="#cdd6f4"
                           FontSize="14" Margin="0,8,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
